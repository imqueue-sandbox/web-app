import React, { Component } from 'react';

/**
 * Car
 */
export default class Car extends Component { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div>Car component</div>
    );
  }
}
